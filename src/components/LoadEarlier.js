/**
 * React Native IM App with DDP(Meteor)
 * the Component which loading earlier messages
 *
 * @zack
 */
import React, { Component } from 'react';
import {
  View,
  ViewPropTypes,
  ListView,
} from 'react-native';
import PropTypes from 'prop-types';

class LoadEarlier extends Component {
  render(){
    return (
      <View>
      </View>
    );
  }
}
